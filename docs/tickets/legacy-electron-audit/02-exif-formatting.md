# 02 — EXIF Formatting (5/10): User-Visible Output Errors, Zero Tests

> **Source:** GitHub issue #28 — https://github.com/Gaotity/yiyin/issues/28 (labels: bug, audit, P1, area:exif)
> **Audit context:** adversarial audit of the pre-rewrite Electron/Svelte tree
> at `7e115bb` (`codex/harden-project-foundation`); that tree was deleted from
> `main` by the Tauri/Rust rewrite (PR #3). Findings quote legacy `file:line`
> paths that no longer exist — re-validate against current `main` before acting.

**What to build:** Fix the EXIF formatting layer, which has user-visible
output bugs and no tests even though the reader is solid (audit dimension
score: **5/10**). The fixes would correct the malformed values users see in
watermarks and put the whole `exif-format` layer under test.

**Blocked by:** None

**Status:** wontfix — superseded by the rewrite; live finding restated in `docs/tickets/post-rewrite-hardening/02-sub-second-shutter-display.md` (triaged 2026-07-29)

- [ ] `ExposureCompensation` prints raw SRATIONAL text, e.g.
  `0.3333333333333333` for 1/3 EV — `common/modules/exif-format/base.ts:92-94`
- [ ] 0.7–0.9s shutter speeds are formatted as `1/1` — `common/modules/exif-format/base.ts:37-40`
- [ ] Add tests for the whole `exif-format` layer (currently zero; excluded from coverage `include`)
- [ ] Delete dead `web/util/model-map.ts` (no importers; logic has drifted, e.g. `α 7m4` with a space)
- [ ] Nikon `Model.replace(Make,'')` without trim → leading space in watermark;
  missing `Model` throws TypeError — `common/modules/exif-format/nikon.ts:7`
- [ ] `Flash()`/`ExposureMode()` are dead methods (fields never extracted by
  the reader) — `base.ts:106-115`
- [ ] `init()` mutates the caller's exif `Make` in place — `common/modules/exif-format/index.ts:34`
- [ ] No i18n layer: English program/metering/white-balance descriptions leak
  into watermarks; `translate.ts` is misnamed (only `toRoman`)
- [ ] `Math.round` returns number, violating the all-string `ExifData` contract
  (hidden by `any`) — `base.ts:54`

## Triage notes

> *This was generated by AI during triage.*

Verified against current `main` (the post-rewrite Tauri/Rust/React tree). Eight of nine findings are already handled or have no analog; the sub-second shutter rounding bug survives and was restated as `post-rewrite-hardening/02-sub-second-shutter-display.md` (ready-for-agent).

- ExposureCompensation raw SRATIONAL → ALREADY-HANDLED — parsed to f64, `decimal()` trims to 6 places, `signed_decimal` adds sign (`crates/yiyin-infrastructure/src/metadata.rs:160-162,395-416`); frozen capture locks `+0.3`
- 0.7–0.9s shutter → `1/1` → LIVE-ANALOG — same reciprocal-round algorithm (`metadata.rs:252-263`); the (0.67, 1.0)s interval is untested → restated in `post-rewrite-hardening/02`
- exif-format zero tests / coverage exclusion → ALREADY-HANDLED — integration tests with frozen-capture golden diff (`tests/metadata.rs`) plus domain tests (`crates/yiyin-domain/src/metadata.rs:168-193`); no coverage config exists
- dead `model-map.ts` → LEGACY-ONLY — no analog; vendor normalization is three tested functions (`tests/metadata.rs:15`)
- Nikon leading space / missing Model TypeError → ALREADY-HANDLED — space trimmed at the render boundary (`crates/yiyin-domain/src/template.rs:310`); missing Model collapses to empty (`template.rs:300-311`); no TypeError concept in Rust
- `Flash()`/`ExposureMode()` dead methods → LEGACY-ONLY — `BuiltInField` has 15 fields; neither exists (`crates/yiyin-domain/src/metadata.rs:36-53`)
- `init()` mutates caller's exif → ALREADY-HANDLED — `normalize_metadata(&Exif) -> Metadata` returns a new value (`metadata.rs:117-184`)
- no i18n / English leaks / `translate.ts` misnomer → ALREADY-HANDLED — program/metering/WB normalized to Chinese labels or symbols (`metadata.rs:337-365`); only `Auto` remains, legacy-parity locked; `to_roman` honestly named
- `Math.round` number vs string contract → ALREADY-HANDLED — `Metadata = BTreeMap<BuiltInField, String>`; all normalizers return `String`, type-enforced
