# 03 — Config & Migration — Failure Path Can Overwrite Real Config with Fake Defaults

> **Source:** GitHub issue #29 — https://github.com/Gaotity/yiyin/issues/29 (labels: bug, audit, P1, area:config)
> **Audit context:** adversarial audit of the pre-rewrite Electron/Svelte tree
> at `7e115bb` (`codex/harden-project-foundation`); that tree was deleted from
> `main` by the Tauri/Rust rewrite (PR #3). Findings quote legacy `file:line`
> paths that no longer exist — re-validate against current `main` before acting.

**What to build:** Harden the legacy config load/migration path so failures
cannot silently destroy user settings: a `getConfig()` failure must not leave
the store `initialized` and later overwrite the real main-process config with
fake defaults, defaults must stop drifting between the web store and the
Electron model, and migration plus corrupt-recovery must degrade gracefully
instead of resetting all blocks or crashing the main process. Dimension score:
**7/10**.

**Blocked by:** None

**Status:** wontfix — superseded by the rewrite; live finding restated in `docs/tickets/post-rewrite-hardening/05-graceful-legacy-config-import.md` (triaged 2026-07-29, amended after codex review on PR #44)

- [ ] `getConfig()` failure still sets `initialized` in `finally`; any later
  edit then overwrites the real main-process config with fake defaults
  (persistent data loss) — `web/store/config.ts:57-70`
- [ ] Default-value drift between web store and electron model
  (`origin_wh_output` true/false, `bg_rate_show` true/false, `options.font`
  `'system-ui'`/`''`) — `web/store/config.ts:20-26` vs
  `electron/config/model.ts:46-53`
- [ ] Migration uses a single `safeParse` over four blocks: one out-of-range
  legacy field resets **all** blocks to defaults —
  `electron/config/migrate.ts:17-28`
- [ ] `FieldInfoItem.font` contract is wider than the schema (all-optional
  type vs required `size`/`font` in `common/config/schema.ts:7-8`)
- [ ] Legacy `version` string is preserved but never updated; no
  version-dispatched migration chain (the hardening plan claims one)
- [ ] Corrupt-recovery catch can re-throw if `writeAtomically` fails (disk
  full/EACCES) → main-process startup crash — `electron/src/config.ts:45-50`
- [ ] Add tests for backup/recovery paths (`writeAtomically`,
  `.invalid.bak`) — currently untested

## Triage notes

> *This was generated by AI during triage.*

Verified against current `main` (the post-rewrite Tauri/Rust/React tree). Six of seven findings are already handled in the rewrite, with tests; one survives in new form and was restated as `post-rewrite-hardening/05-graceful-legacy-config-import.md` (ready-for-agent).

- getConfig failure → fake defaults overwrite real config → ALREADY-HANDLED — bootstrap failure enters the error phase with no config to edit (`src/app/useAppController.tsx:53,256-257`); corrupt load recovers via `.invalid.bak`/`.bak` preserving the corrupt bytes (`crates/yiyin-infrastructure/src/config/json_repository.rs:63-98`), tested (`tests/config_persistence.rs:212-230`)
- default drift between web store and electron model → ALREADY-HANDLED — single source `RenderOptions::default`/`Config::default` in domain (`crates/yiyin-domain/src/config.rs:184-208,243-247`); UI receives DTOs from Rust
- single safeParse resets all blocks on one bad field → LIVE-ANALOG — source preservation is handled (legacy import fails atomically and never mutates the source), but one invalid legacy field still fails the entire legacy decode and aborts startup via `AppState::compose` (`crates/yiyin-infrastructure/src/config/legacy_import.rs:87`, `src-tauri/src/state.rs:194`) → restated in `post-rewrite-hardening/05` (verdict corrected after codex review on PR #44)
- `FieldInfoItem.font` wider than schema → ALREADY-HANDLED — `FontSpecDto` requires all fields; `StoredFieldFont.into_domain` validates at the boundary (`json_repository.rs:633-648`)
- version never updated, no migration chain → ALREADY-HANDLED — versioned envelope stamped on every write; non-current versions rejected (`json_repository.rs:196-202,276-291`), tested (`config_persistence.rs:233-246`)
- corrupt-recovery re-throw → main-process crash → ALREADY-HANDLED — recovery errors surface as `ApplicationError` to the error phase; atomic-write fault injection proves prior config survives (`config_persistence.rs:248-266`)
- no tests for backup/recovery → ALREADY-HANDLED — `tests/config_persistence.rs` and `tests/legacy_import.rs` cover round-trip, `.bak` preservation, `.invalid.bak` recovery, and future-schema rejection
